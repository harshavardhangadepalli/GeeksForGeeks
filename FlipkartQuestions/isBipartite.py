class Solution:
    def isBipartite(self, V, adj):

        def bfs(i):
            if colours[i] == 1

        # code here
        colours = list()
        # unvisited = 0
        # colour 1 = 1
        # colour 2 = 2
        for i in range(V):
            colours[i] = 0
        for i in colours:
            if colours[i] == 0:
                bfs(i)